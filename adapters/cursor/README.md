# Cursor Adapter

Cursor integrations should reference the existing `skills/` tree and surface `config/package_manifest.json` as the product and edition source of truth.

Keep this adapter as configuration guidance only. Business logic remains in shared scripts and skill instructions.
