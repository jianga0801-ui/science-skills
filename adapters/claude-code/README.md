# Claude Code Adapter

Claude Code should read each `SKILL.md` from `skills/` and use `metadata.json` plus `config/package_manifest.json` for package identity and edition eligibility.

This adapter is a thin mapping layer. It does not fork or rewrite the underlying skills.
