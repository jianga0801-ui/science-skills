# Codex Adapter

Codex loads the bundle from the repository structure directly. Use `config/package_manifest.json` to determine the active edition and allowed delivery surface.

This adapter only documents loading behavior and must not copy skill scripts or prompt logic.
